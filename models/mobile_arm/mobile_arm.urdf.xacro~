<?xml version="1.0"?>
<robot xmlns:xacro="http://ros.org/wiki/xacro"
       name="mobile_ur10">

  <!-- mobile base -->
  <!-- include xacro file containing a link where you want to attach the manipulator -->
  <xacro:include filename="$(find creeling03)/models/plugin_robot_02/ddbot.xacro"/>

  <!-- arm -->
  <!-- currently the following file just loads gazebo ros_control plugin -->
  <xacro:include filename="$(find ur_description)/urdf/common.gazebo.xacro" />

  <!-- ur10 -->
  <xacro:include filename="$(find ur_description)/urdf/ur10.urdf.xacro" />  
  <xacro:ur10_robot prefix="" joint_limited="false"/>

  <!-- joint to attach ur10 to mobile base -->
  <joint name="arm_to_mobile_base" type="fixed">
    <origin xyz="0 0 ${.352 / 2}" rpy="0 0 0" />
    <parent link="chassis" />
    <child link="base_link" />    
  </joint>

  <!-- to see if ur10 will slide -->
      
</robot>
